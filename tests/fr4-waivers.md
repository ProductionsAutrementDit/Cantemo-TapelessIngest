# FR-4 equivalence waivers

FR-4 requires the refactored pipeline to behave equivalently to the legacy
one. Every DELIBERATE, signed-off divergence is declared here — one row per
behavioral delta, added by the story that introduces it, together with the
pinning-test update and the `tests/pinned-bugs.md` row it retires.

A delta absent from this table is a regression, not a decision.

| FR | story | date | behavioral delta |
|----|-------|------|------------------|
| FR-10 | 2.4 | 2026-08-21 | Scan now materializes `Clip`/`ClipMetadata` rows for never-ingested clips (a hash-recovered `item_id` therefore lands on the row at scan time instead of at ingest time). Pre-2.4 only an already-saved clip's metadata was written, per key, by the `metadatas` setter. |
| FR-10 | 2.4 | 2026-08-21 | Re-scanning a never-ingested folder reports `created=0` instead of re-counting the same N clips on every run — the first scan now persists them. More honest; the FR-4 counter tuples of a first scan are unaffected. Pinned by `tests/tier2/test_persistence_write_unit.py::test_rescan_never_ingested_reports_created_zero`. |
| FR-11 | 2.4 | 2026-08-21 | A dry-run scan no longer writes the `Folder` row nor an existing clip's metadata rows. Pre-2.4 `dry_run` gated only `Folder.ingest`'s ingest block, so a "dry" run still wrote both. Directionally FR-11; full dry-run purity is story 2.7. Pinned by `tests/tier2/test_persistence_write_unit.py::test_dry_run_scan_writes_nothing`. |
| FR-23 | 2.5 | 2026-08-21 | `already_ingested` now counts clips whose `item_id` is truthy (post hash-recovery) instead of every clip the page processed. Pre-2.5 it counted `clip.file is not None`, which the scan sets for every clip it touches, so the counter reported the page size. Reported values drop for folders of never-ingested clips; the ingest counters are unaffected. Pinned by `tests/tier2/test_scan_counters.py::test_single_page_scan_counters`. |
| FR-8 | 2.5 | 2026-08-21 | A file Cantemo has not hashed yet is SKIPPED with a logged retry-next-run reason instead of raising `TapelessIngestException("No hash found in file …")`. Pre-2.5 the exception cost the file its clip, its metadatas and its scan record, and appeared in `errors`; it is now scanned normally, counted `skipped` by the ingest ladder, never matched against a legacy storage and never ingested (NFR-1 ruling: the hash is the dedup key, ingesting without it risks a duplicate item). The next cron run retries it once the hash exists. Pinned by `tests/tier2/test_ingest_discipline.py::test_hash_less_file_is_skipped_with_a_retry_reason`. |
| FR-8 | 2.5 | 2026-08-21 | A recovered `item_id` is now assigned to PRE-EXISTING clip rows as well, not only to rows being created. Recovery fires only when the clip has no `item_id`, so it can never overwrite a known one. Since the 2.4+2.5 review round the scan also PERSISTS it, through a fill-only-NULL statement of its own (`PersistencePlan.recovered_item_ids`) rather than through `CLIP_UPDATE_FIELDS`: the ingest ladder skips a clip the moment its `item_id` is truthy, so `Clip.ingest` — the other writer of that column — is never reached for exactly these clips, and the id would have been recovered again on every future scan. Pinned by `tests/tier2/test_ingest_discipline.py::test_recovery_assigns_the_item_id_to_a_pre_existing_row`, `::test_a_recovered_item_id_is_persisted_on_the_row`, `::test_a_second_scan_no_longer_pays_the_recovery_lookup` and `::test_a_recovered_id_never_overwrites_an_id_the_row_already_has`. |
| FR-35 | 2.5 | 2026-08-21 | `_should_replace_original_files` no longer auto-skips every non-replace call: the guard was `len(original_files) >= 0`, always true. An item whose original shape holds NO file now falls through to the remaining checks (same file id / same storage / non-legacy storage) instead of being declared already-imported. Reachable ONLY on the incomplete-import retry path below (with `replace=False` and no retry, `import_file` returns at the "item already exists" rung before this function is called; with `replace=True` the guard's own `replace == False` conjunct short-circuits it). Pinned by `tests/tier1/test_scan_ingestion.py::test_empty_original_files_no_longer_auto_skips` and, end to end, `tests/tier2/test_ingest_discipline.py::test_an_empty_original_shape_does_not_block_the_retry`. |
| FR-36 | 2.5 | 2026-08-21 | A clip whose import created a placeholder but started no job (`item_id` set, `job_id` NULL, status `Placeholder created`) is RE-INGESTED on the next run instead of being skipped as already-ingested forever. The retry passes `retry_incomplete=True`, which lifts `import_file`'s blanket "item already exists" early return and nothing else — an item that really holds original files still comes back `skipped`, and the same placeholder is reused rather than a second one created. Deliberately narrower than "item_id without job_id": a HASH-RECOVERED id has no job either, and retrying those would buy back the per-clip HTTP cost FR-8 removed. Pinned by `tests/tier2/test_ingest_discipline.py::test_an_incomplete_import_is_retried_next_run_not_skipped_forever`, `::test_a_recovered_item_id_is_not_mistaken_for_an_incomplete_import` and `::test_an_item_that_already_holds_the_file_is_not_re_imported`. |
| FR-23 | 2.5 | 2026-08-21 | Two scanned files resolving to ONE umid produce one ingest, not two. The umid is the clip's primary key, so they are one clip by definition; the write side already collapsed them (`dedupe_candidates`) while the ingest side did not. `created` likewise counts a umid once even when its files span two pages. The scan RESPONSE still reports every file, unchanged. Pinned by `tests/tier2/test_ingest_discipline.py::test_two_files_one_umid_are_ingested_once` and `::test_a_umid_spanning_two_pages_is_created_once`. |
| FR-10 | 2.5 | 2026-08-21 | `Clip.ingest` writes back only `INGEST_STATE_FIELDS` (`item_id`, `job_id`, `status`, `file_id`, `user`) where the deleted full `save()` wrote every column. For an EXISTING clip whose scanned file changed provider or reference file, an ingest therefore no longer rewrites `provider_name`/`reference_file` (nor location columns, which the scan deliberately never rewrites either). Everything `import_file` mutates is in the list; the dropped columns are ones only a scan produces. Pinned by `tests/tier2/test_ingest_discipline.py::test_ingest_writes_only_ingest_state_columns`. |
| FR-12 | 2.4 | 2026-08-21 | A non-empty `clip_xml` is authoritative and is never re-read from the card: a CORRECTED sidecar on disk is ignored for the life of the row (only deleting the column, or the clip, re-reads it). That is the point of the column — the pre-2.4 property re-read and re-parsed the sidecar on every save — but it is a real behavioral delta, not just a cache. Pinned by `tests/tier2/test_persistence_write_unit.py::test_a_stored_clip_xml_is_never_re_read_from_the_card`. |
| FR-27 | 2.4 | 2026-08-21 | Duplicate `Folder` rows (the NULL-`storage_id` loophole in the unique constraint) no longer raise `MultipleObjectsReturned` out of `get_or_new`: the first row by pk is returned with a WARNING. If the duplicates vanish between the failed `get()` and the retry read, a new unsaved instance is returned rather than `None`. Pinned by `tests/tier2/test_persistence_write_unit.py::test_duplicate_folder_rows_resolve_first_by_pk`. |
| FR-28 | 2.6 | 2026-08-21 | An unresolvable storage no longer raises `AttributeError` out of the run. `Folder.root_path` returns `None` when `browse_root_path` cannot resolve one (pre-2.6 `_root_path` was simply left unassigned), so `absolute_path` is falsy and the folder returns a NORMAL response carrying `Cannot get full path from storage {id}, path {path}`. Covers BOTH deltas: `Folder(storage_id=None)` and a context whose storage is unresolvable (`root_path=None` → property-chain fallback). The recursion reports the same message and stops at that folder instead of calling `os.scandir(False)`. Pinned by `tests/tier1/test_scan_pagination.py::test_storage_none_reports_cannot_get_full_path` and `tests/tier1/test_scan_adapters.py::test_scan_with_ctx_miss_falls_back_to_property_chain`. |
| FR-24 | 2.6 | 2026-08-21 | The recursion no longer descends into SYMLINKED subdirectories. It lists children through `FolderListings.dirs`, which is built with `follow_symlinks=False` (story 2.2's cycle guard), where the pre-2.6 `os.scandir` loop used `entry.is_dir()`, which follows links. A symlink loop can no longer make the walk recurse forever, and a symlinked card folder is scanned once — through its real path — instead of once per link pointing at it. |
| NFR-5 | 2.6 | 2026-08-21 | `Folder.scan`/`Folder.ingest` responses carry a new `consumed_subdirs` key (FR-19): the immediate child names this folder's clips consumed, or `None` for DOUBT ("descent is not authorized for this folder"). Purely ADDITIVE — no pre-existing key is renamed, removed or changed — so the frozen key-set pins were extended rather than broken: `tests/tier2/test_scan_counters.py`, `tests/tier2/test_scan_once_per_run.py`, `tests/tier2/test_scan_verification_integration.py`, `tests/tier1/test_scan_pagination.py`. |
| FR-11 | 2.7 | 2026-08-21 | Dry-run ingest counters report WOULD-BE outcomes instead of baseline zeros. The 2.5 ladder is pure, so it now runs and is COUNTED in both modes: `skipped` carries the ladder's rejections (item_id-bearing with `replace=False`, hash-less) and `ingested` is `len(to_ingest)` — every clip the run would have submitted, replacements included. `failed`, `replaced` and the share of `skipped` that `Clip.import_file` decides on its own remain 0 STRUCTURALLY, because no submission occurs; whole-dict equality with a real run is NOT claimed. The relation is: dry-run `ingested` is an upper bound on a real run's, dry-run `skipped` a lower bound, coinciding exactly when every submission succeeds. Supersedes the `<frozen-after-approval>` dry-run boundary of approved spec 2.5 ("skipped/ingested/failed/replaced stay 0,0,0,0 (2.7 boundary)"), recorded in both specs' Spec Change Logs. Pinned by `tests/tier2/test_dry_run_purity.py::test_dry_run_counters_match_real_run`, `::test_a_post_ladder_skip_shows_the_bound_not_the_equality` and the re-baselined `tests/tier2/test_scan_counters.py::test_ingest_dry_run_key_superset` (0,0,0,0 → 1,1,0,0) and `tests/tier2/test_scan_once_per_run.py::test_tree_run_resolves_each_storage_once` (0,0,0,0 → 1,0,0,0 — the same superseded clause in a second place the 2.7 spec's Code Map did not enumerate). |
| FR-31 | 2.7 | 2026-08-21 | **(SUPERSEDED — see the FR-31/2.8 row below for the string that actually ships.)** The end-of-run summary of a `--dryrun` run reads `DRY-RUN: {count} folders scanned — no changes were made` instead of `{count} folders scanned` (real runs unchanged). Edited identically in both commands, so it also reaches Slack through `CustomLogger`. Pinned by `tests/tier2/test_dry_run_purity.py::test_summary_line_is_labelled_under_dryrun` and, for symmetry, `tests/tier1/test_commands_in_sync.py::test_handle_sources_identical`. |
| FR-36 | 2.5 | 2026-08-21 | An import response carrying no `jobId` is counted `failed`, never `ingested`. Pre-2.5 `import_file` fell through to an unconditional `result["ingested"] = True`, and `_import_multi_component` returned `True` whatever the response held — so a clip with a NULL `job_id` and no import job could be reported ingested. Pinned by `tests/tier2/test_ingest_discipline.py::test_multi_component_import_without_a_job_id_is_not_an_ingest` and, through the real `import_file`, `::test_a_real_import_without_a_job_id_is_failed_never_ingested`. |

| FR-31 | 2.8 | 2026-08-21 | **Supersedes the FR-31/2.7 row above.** The end-of-run summary moved out of `Command.handle` into the coordinator (`scan/coordinator.py::summary_lines`) and became TWO lines, both carrying the `DRY-RUN: ` prefix when `ctx.options.dry_run` is set: `{prefix}{folders_scanned} folders scanned, {folders_failed} failed in {elapsed:.1f}s — discovery …s, verification …s, extraction …s, persistence …s, ingest …s, other …s` and `{prefix}{hits} clips found, {created} created, {already_ingested} already ingested, {processed} processed, {ingested} ingested, {skipped} skipped, {failed} failed, {replaced} replaced, {n} errors`. 2.7's explicit "— no changes were made" reassurance is GONE; the `DRY-RUN: ` prefix is what marks a rehearsal, on both lines, and it still reaches Slack through the same `CustomLogger` path. `other` is the wall clock minus the five phase totals, clamped at zero, so the line reconciles. Pinned by `tests/tier2/test_dry_run_purity.py::test_summary_line_is_labelled_under_dryrun` (both commands x both flag states, full-literal match) and `tests/tier1/test_scan_coordinator.py::test_the_phases_and_other_reconcile_with_the_elapsed_time`. |
| FR-37 | 2.8 | 2026-08-21 | `check_clips_in_folder` is read-only BY CONSTRUCTION (`FORCE_DRY_RUN = True`), so its summary is `DRY-RUN:`-labelled whether or not `--dryrun` was passed — where the 2.7 row implied the label tracks the flag. Its `--dryrun`/`--replace` flags are kept for sibling symmetry and announce themselves inert once at the start of every run. Pinned by `tests/tier2/test_check_clips_in_folder.py`. |
| FR-20 | 2.6 | 2026-08-21 | **(Row added by the Epic 2 final review round — the delta shipped with 2.6 and had no waiver.)** `--skip` and `--only` are evaluated at EVERY depth of the walk; the pre-2.6 recursive call forwarded neither, so they only ever filtered depth-1 shoot folders. `--startWith` and the `--from`/`--since` window stay depth-1-only. This is operationally SHARP and not merely a tightening: `--only 2022` used to select shoot folders and then scan them whole, and now also has to match every subdirectory below them, so a card layout like `AH_20220101_shoot/CARD/CLIP` stops being descended into. The `--skip`/`--only` help strings say so. Pinned by `tests/tier2/test_selective_recursion.py::test_only_filter_applies_at_depth_without_a_date_window` and `tests/tier1/test_scan_coordinator.py::test_skip_and_only_apply_at_every_depth`. |
| NFR-1 | 2.8 | 2026-08-21 | **(Epic 2 final review round.)** Four hardenings of the descent barrier, all in the over-consuming (safe) direction: (1) a folder whose write unit ROLLED BACK yields no clips and does not authorize descent — pre-fix its clips were still submitted, and `Clip.ingest`'s fallback `save()` would have created a Vidispine item no row recorded; (2) descent now requires the pages to have actually consumed every hit the index reported, not merely that the page loop ended — a SHORT page used to leave the completeness flag true; (3) `consumed_subdirs`' layer (b) is fed from every provider that CONTRIBUTED to a clip rather than from `metadatas["provider"]` (last-writer-wins), so a co-matching provider's sub-path layout is no longer left open; (4) clips that can name no directory at all are DOUBT, not an empty consumed set. Additionally the walk dedupes by `os.path.realpath`, so one physical directory reachable by two paths (bind mount, hardlinked directory) is walked once. Pinned by `tests/tier2/test_epic2_review_pins.py` and `tests/tier1/test_extraction_hardening.py`. |
| FR-22 | 2.8 | 2026-08-21 | **(Epic 2 final review round.)** A root `--path` that cannot be listed or resolved is now COUNTED as a failed folder and lands in `errors`, where it was previously printed and then discarded — a run over a missing root reported "0 folders scanned, 0 failed", indistinguishable from an empty tree. Both commands additionally exit non-zero (`CommandError`) when `folders_failed` is non-zero or the walk itself raised, after flushing Slack. Pinned by `tests/tier1/test_scan_coordinator.py::test_an_unresolvable_root_is_counted_as_a_failure_not_just_printed` and `tests/tier2/test_epic2_review_pins.py::test_a_failed_folder_gives_the_cron_a_non_zero_exit`. |
| AD-14 | 2.8 | 2026-08-21 | **(Epic 2 final review round.)** `TREE_ONLY_OPTIONS` ships seeded with `skip`, `only`, `startwith`, `date_window` instead of empty: a paged call scans one folder and never walks, so any of the four could only mislead a caller into believing a narrowing had been applied. `Folder.scan`/`Folder.ingest` raise `TapelessIngestException` when handed a context carrying one. Prod is unaffected — tree mode reaches the pipeline through `process_folder`, not through the façades. Pinned by `tests/tier1/test_scan_coordinator.py::test_a_walk_filter_is_rejected_in_paged_mode`. |

| AD-4 | 2.8 | 2026-08-21 | **(Epic 2 final review round.)** `RunOptions.providers` and `.legacy_storages` are TUPLES, not lists: two mutable fields on a frozen dataclass whose four newest fields were tuples for exactly that reason. `build_context` and `build_default_context` freeze the caller's lists at construction, so both entry modes carry the same type and no caller can mutate a "frozen" run option under the walk's feet. `None` is PRESERVED and is not `()` — `build_provider_registry` reads `None` as the canonical `PROVIDER_NAMES` tuple, while an empty tuple is an empty registry, i.e. a run that matches nothing. Pinned by `tests/tier1/test_scan_adapters.py::test_build_context_wires_storages_and_options` and `::test_none_providers_survive_the_freeze_as_none`, and `tests/tier2/test_scan_once_per_run.py::test_paged_ingest_default_ctx_carries_actual_options` (all three re-baselined list -> tuple). |

| FR-22 | 2.3+2.6 | 2026-08-21 | **(Production dry-run fix, post-Epic-2 — it scopes what FR-22 reports, so it is a decision, not a hole.)** `FolderListings.errors()` no longer reports ONE combination: a directory that only ever received a SPECULATIVE provider probe (`Provider.probe_is_file`, now `probe=True`) and whose listing failed with `ENOENT`/`ENOTDIR`. A card provider guarded on sidecar presence probes its sibling layout (`panasonicP2`'s `../CLIP/{name}.XML`, `ikegami`'s `../CLIPINF/CLIP{name}.XML`) for every file it is offered; story 2.3 routed those probes through the listings (FR-16) and story 2.6 surfaces every recorded scandir failure (FR-22), so the composition made every folder whose card type does not match a probing provider emit one phantom `Error listing directory …: [Errno 2] No such file or directory` per probed sibling — the real DJI dry run reported `1 errors encountered` on a clean folder, and the cron's thousands of weekly folders would have made the error COUNT that 2.6/2.8 built worthless. Both other axes are UNCHANGED and pinned as such: a probe into a directory that exists but cannot be READ (`EACCES`, any non-absence `OSError`) is still reported — the provider silently did not match a card it may have had to match — and a listing the SCAN required (the worker verifying an indexed file, the walk listing a folder it is about to descend into) is still reported whatever the errno, `ENOENT` included. The suppressed set stays auditable via `FolderListings.probe_absences()` and is logged at DEBUG by the worker. Pinned by `tests/tier2/test_probe_listing_errors.py` (all four cells, end to end through `Folder.scan`) and `tests/tier1/test_scan_verification.py::test_a_probe_into_a_missing_directory_is_not_reported`, `::test_a_probe_into_an_unreadable_directory_is_still_reported`, `::test_a_scan_required_missing_directory_is_reported`, `::test_a_probed_then_required_directory_becomes_reportable`, `::test_a_required_then_probed_directory_stays_reportable` and `::test_a_failed_listing_records_the_errno_alongside_the_message`. |

| FR-19 | 2.6 | 2026-08-24 | **(Epic 2 retrospective, action item 3 — post-Epic-2. It closes the one case the three-state descent gate never asked about, so it is a decision, not a hole.)** A folder that VERIFIED files, assembled NO clip from them, and recorded at least one error no longer returns an empty consumed set (`descend into everything`) — it returns DOUBT, and the walk stops there with `Not descending into {path}: {n} verified file(s) found, none became a clip, {m} error(s) — what this folder holds is unknown`. Everywhere else `consumed_subdirs` is already careful that doubt is `None` and never `frozenset()`; zero clips was the one path that fell through. It matters because a card folder whose extractor broke is indistinguishable at that gate from an intermediate shoot folder, and the empty set sends the walk through the card's internals: measured on the REDline outage (`e6d0090`, the binary absent from cron's PATH), 118 folders / 584 clips / 556 errors where a correct run reports 49 / 203 / 0. Two boundaries are RULED and pinned as such, both in the not-firing direction. (1) The count is of files that PASSED the real-filesystem guard, not of rows the index returned: a ghost entry (index/filesystem desync, DC-2) yields a hit row and an error while there was never media to claim, and the guard absorbing it is FR-22 working as designed — the first draft counted rows and `tests/tier2/test_selective_recursion.py::test_independent_subtrees_are_scanned_and_nothing_is_ingested_twice` and `tests/tier2/test_coordinator_tree.py::test_a_zero_hit_folders_errors_are_surfaced_and_it_still_counts` both caught it. (2) Doubt additionally REQUIRES a recorded error: a stray file every provider's guard legitimately declines is an unclaimed hit with nothing wrong, and must not stop the walk above real cards — the wider rule (doubt on any unclaimed hit) was considered and rejected. Zero-hit folders are untouched and still descend, which is how the walk finds cards at all. Pinned by `tests/tier1/test_unclaimed_hits_doubt.py` (all five cells of the predicate) and `tests/tier2/test_unclaimed_hits_descent.py` (end to end through `Folder.scan` and through the real walk, including the ghost and zero-hit non-regressions). |

| NFR-1 | 3.0 | 2026-08-25 | **(No orphaned placeholders — the combined write.)** `Clip.create_item` now persists `item_id` AND `status = PLACEHOLDER_CREATED` together, in one targeted UPDATE by pk, immediately after `createPlaceholder` and before any further Vidispine call — where before NOTHING was written until `persist_ingest_state` ran after `import_file` returned. A crash-time observer therefore sees two columns change mid-import, and a process death (or any raise) anywhere after that one statement leaves exactly the FR-36 incomplete-import cell, which the EXISTING 2.5 retry rung recovers into the SAME placeholder next run — no marker, no new status, no claim. The write no-ops when no row exists (the REST path ingests an unsaved `Clip`; never a `save()` fallback). The promise is stated honestly: a death INSIDE `createPlaceholder` (item created server-side, response lost) still orphans, and the next run still creates a second placeholder — the unnamed-orphan window is ONE statement, not zero; the residual is accepted and owned by the dedicated maintenance story. Pinned by `tests/tier2/test_ingest_discipline.py::test_a_death_after_the_combined_write_retries_into_the_same_placeholder` (three windows), `::test_a_death_at_the_import_call_itself_comes_back_skipped_not_doubled` (Vidispine attached the file, so the honest run-2 answer is `skipped`, and the job id is lost for good — which also leaves a PERMANENTLY HOT row: it keeps reading as the FR-36 cell, so every future run re-selects it and pays `create_item` plus a shape query to conclude `skipped` again, forever; accepted, and one more reason the dedicated maintenance story owes operator tooling), `::test_a_recovered_submission_reuses_the_placeholder_shape_it_left`, `::test_a_death_before_create_placeholder_retries_cleanly`, `::test_a_death_inside_create_placeholder_is_the_accepted_residual` (the residual, pinned as a document, not a promise), `::test_a_rest_ingest_of_a_new_clip_never_inserts_before_the_import` and `::test_a_rest_ingest_of_a_new_clip_proceeds_without_crying_wolf`. No migration: `status` gains no constant (`::test_makemigrations_sees_no_model_change`). |

| FR-23 | 3.0 | 2026-08-25 | **(No orphaned placeholders — the refusal, and the per-clip `failed` count.)** A row that NAMES an item Vidispine cannot resolve is refused at the top of `create_item`'s create branch instead of falling into it: `Clip.item` answers `None` on `NotFoundError` too (the accident commit `632bd4c` closed), so a deleted item used to be silently RE-CREATED — a second placeholder beside the one the row still names. The refusal raises `TapelessIngestException`; the per-clip handler in `Folder._ingest_pass` catches it, counts the clip `failed` and puts the reason in `errors`, every run, until the row is healed (`--replace` included; operator tooling belongs to the maintenance story). Alongside it, that handler now counts EVERY per-clip exception as `failed` — before, a clip whose submission raised was counted nowhere (not ingested, not skipped, not failed) and the summary's arithmetic quietly lost it. All predicates test FALSINESS of `item_id`, never `is None`: the deletion listener writes `""`, and `Clip.item`'s own `is None` test became a falsiness test so `""` stops reaching `getItem("")`. Pinned by `tests/tier2/test_ingest_discipline.py::test_a_row_naming_an_unresolvable_item_is_refused_not_recreated` and `::test_an_empty_item_id_never_reaches_getitem`. |

| FR-31 | 3.0 | 2026-08-25 | **(No orphaned placeholders — the listener.)** `utilities.ClipResetWithItemDeletion` now also sets `status = STATUS_NOT_IMPORTED`, which it had logged ("Put clip … status as Not Imported") and never done since it was written. Deleting the orphaned item in Portal is the natural manual repair for an interrupted submission — which since the combined write sits at PLACEHOLDER_CREATED — and without the reset the repair left PLACEHOLDER_CREATED beside an empty `item_id`: re-ingested anyway (the id columns were cleared), but displayed as an import that never happened. Only the displayed status changes. Pinned by `tests/tier2/test_item_deletion_listener.py::test_deleting_the_item_also_resets_the_status`. |

| FR-18 | 3.2 | 2026-08-26 | **Supersedes story 1.5's abort-on-first-failure Design Note — for the 429 case ONLY.** `CustomLogger.send_messages_to_slack` (both command modules, identical) now retries a chunk that fails with a `SlackApiError` whose `response.status_code == 429` against the SAME chunk, up to `SLACK_RATE_LIMIT_RETRIES` (3) times, honouring a case-insensitive `Retry-After` header (missing/garbage → 1.0 s; capped at 30 s), each retry logged at WARNING; successful sends are paced by `SLACK_SEND_PACE_SECONDS` (1.0 s) between chunks, never after the last. Everything else keeps 1.5's contract: any FATAL error (non-429 `SlackApiError`, network, auth) still abandons on its first throw with the client called exactly once; exhausted retries and fatal errors alike produce exactly one ERROR record (`Slack notification failed on chunk {k}/{n}; remaining chunks abandoned`, exc_info) and return `None`, a notification failure never propagates into the scan run, and the chunking algorithm, the three skip-path INFO strings and the success INFO are untouched. The 1.5 pin `tests/tier1/test_slack_notification.py::test_send_failure_is_swallowed_and_logged` (which asserted `client.calls == 1` for EVERY exception) was deliberately rewritten as fatal-only; the 429 behavior is pinned by the new retry/pacing tests in the same file, and the constants/helpers by the extended `::test_slack_block_identical_across_commands`. |

Retires: `tests/pinned-bugs.md` row #4 (scan writes the folder row). The
surviving half of that row — a scan is still not read-only, it still
persists `provider_names`/`scanned_on` — stays pinned in
`tests/tier2/test_scan_counters.py::test_single_page_scan_counters`, now
together with the write-once and zero-hit-no-row guarantees.

Retires: `tests/pinned-bugs.md` row #1 (`already_ingested` inflation),
per the FR-23 row above.

Retires: `tests/pinned-bugs.md` row #5 (`storage=None` raises
`AttributeError`), per the FR-28 row above. Its pinning test
(`test_storage_none_raises_attributeerror`) was rewritten as
`test_storage_none_reports_cannot_get_full_path`, and the two story-2.1
adapter tests that asserted the same `AttributeError` were updated with it.


| FR-4 | red-segments | 2026-08-28 | **(RED: segment grouping and optional card structure — the DISCOVERY delta.)** The set of files `red` discovers changed, deliberately, in three directions. (1) WIDER PARENT: `getFilters()`'s `regexp` on `parent` is now `{folder}/[^/]+\.[Rr][Dd][MmCc](/[^/]+\.[Rr][Dd][MmCc])?` where it was `{folder}/[A-Z][0-9]{3}_[0-9A-Z]{6}.RDM/[A-Z][0-9]{3}_[A-Z][0-9]{3}_[0-9A-Z]{6}.RDC`. Card structure is now OPTIONAL and NON-DISCRIMINATING — a folder whose name ends in `.RDM` or `.RDC` is enough, EITHER level may be EITHER extension, and an `.RDC`'s copy suffix (`…_002.RDC`, `…_S000.RDC`) never discriminates. (2) EITHER CASE, both in that pattern and in the name clause, because the copies this story exists for were made any which way. (3) WIDER NAME: the `wildcard` on `name` became a nested `bool.should` of `*.R3D` and `*.r3d` where it was a single `*_001.R3D` — selecting the anchor stopped being a query concern. Rushes copied loose used to miss the precise pattern and fall through to the bare `.r3d` extension declaration, where EVERY segment was probed as its own clip candidate: 132 such `.RDC` folders across five shoots spanning 2022-2026 (CONDOR, NH90 SEA TIGER x2, A321XLR CARAVAN, STARLUX), measured on prod 2026-08-27, of which 24 clips ended up anchored on a middle segment. "Ends in `.RDM`/`.RDC`" is the convention PAD's production server already runs on — its `collections_ignore_folder` SETTING is configured with `.+\.RDM`/`.+\.RDC` — which is corroboration, not a shipped default: `models/settings.py` defaults that field to `""`, and no code path may assume it. Consequences, all intended: a card folder's reported `hits` is now every segment (30 for a canonical 30-segment card, where it was 1); `processed`, `created` and `already_ingested` are UNCHANGED for a canonical card and DROP for a loose one (13 -> 1 on the measured shape); and a loose card is now claimed from its parent shoot folder and consumed as a subdir (FR-19/AD-8) where the walk previously descended into it. `Clip.umid` is untouched. Pinned by `tests/tier1/test_red_segment_grouping.py` and `tests/tier2/test_red_card_shapes.py` (every shape under BOTH `--discovery` modes). `tests/fixtures/golden_search_doc.json` was deliberately re-recorded twice, with human sign-off, for exactly these two clauses; `tests/tier2/test_index_discovery_equivalence.py`'s `_red_filters` fixture now IMPORTS `providers.red.CARD_SUBPATH_REGEXP` rather than copying it. |

| FR-19 | red-segments | 2026-08-28 | **(RED: segment grouping — the ASSEMBLY rules.)** New behaviour with no pre-image. `models/clip.py::segment_role` classifies every discovered file whose suffix an APPLICABLE provider declares through the new `Provider.getSegmentedExtensions()` hook (`red`: `.R3D`; no other shipped provider declares one). Rules, in order: (1) `X_001.R3D` anchors a clip and `X_002…X_NNN` are its extra files, skipped BEFORE extraction and before `processed` is incremented; (2) a name carrying no three-digit increment — or carrying one with nothing else of its stem beside it, a lone `SHOT_042.R3D` — is its own clip; (3) an increment with no `_001` AND at least one other increment of the same stem beside it is an incomplete copy: reported, never a clip. Rule 3's second condition is Camille's 2026-08-28 amendment to the frozen spec; as first written the rule made every lone three-digit filename a permanent per-scan error that was never ingested. Four properties are load bearing and each is pinned: (a) matching is CASE-SENSITIVE and a provider declares the case its own guard accepts — `red` guards on `== ".R3D"`, so `x_002.r3d` is UNGROUPED and keeps being claimed by the `file` provider exactly as before; lowercasing it would suppress siblings that the claiming provider has no `getClipAdditionalMediaFiles` to restore, silently deleting a card's media; (b) the suffixes binding on a file are its APPLICABLE providers' only (`segment_suffixes_for`), so a provider cannot suppress a file it is never offered; (c) the anchor and the sibling set are read from the real FILESYSTEM through the scan's `FolderListings`, so a card whose segments cross a page boundary groups identically; (d) `red.getClipAdditionalMediaFiles` derives its glob from the anchor's ON-DISK stem — never from REDline's CSV `Clip Name`, which diverges for renamed rushes and used to select nothing — and holds the results to `<stem>_[0-9]{3}<same extension>`, so a neighbouring clip whose stem is a prefix (`A001` vs `A001_B`) is not swallowed; hitting the 1000-file page limit is now reported instead of silently truncating a clip's media. Reporting is ONE entry per directory, logged at WARNING without a traceback: an incompletely copied 30-segment card is one data condition, not 30 scan failures, and one error rather than 30 keeps `unclaimed_hits_doubt`'s input honest. Two knock-on effects are accepted and pinned rather than hidden: a folder whose media is ALL orphans records an error, produces no clip and therefore refuses descent; and an orphan card claimed from its parent is reported by the parent and again when the walk descends into it (over-REPORTING, never over-ingesting). A separate guard covers the DC-2 desync grouping straddles: extras skipped in a directory no clip of this folder claimed are reported, so an anchor that is on disk but absent from the index can no longer make a whole clip's media vanish with zero clips and zero errors. This story uses EXTRAS and deliberately does not touch `Clip.spanned`/`spanned_order`/`spanned_id`/`master_clip` or `getSpannedClips()`, which exist for a take split across several PHYSICAL cards (N linked rows with a master) rather than one take's media split into files (one row, N files) — Camille's 2026-08-28 ruling, pinned by `::test_grouping_never_touches_the_spanned_fields`. |

| NFR-4 | red-segments | 2026-08-28 | **(RED: segment grouping — the operator-visible report.)** The per-folder log line reads `found {n} files in {path}, …` where it read `found {n} clips in {path}, …`. It was always the DISCOVERY count, and segment grouping makes the misnomer material: a canonical 30-segment card is 30 files and one clip. The clip count is the `{n} created` field two positions along, unchanged. Both commands emit through the same `_folder_worker` line, so Slack and the cron log change together. Grouping is timed under `extraction` — it is clip assembly, and the directory listing it reads was already paid for by the DC-2 verification guard — rather than under `verification`, so the phase totals stay meaningful; no timing phase was added, because `TIMING_PHASES` feeds the FR-31 summary line. Re-baselined in `tests/tier2/test_coordinator_tree.py`, `::test_selective_recursion.py`, `::test_check_clips_in_folder.py` and `::test_index_discovery_equivalence.py`. **Downstream note:** `views.py`'s browse endpoint computes `pages = hits / number` from the same count, so a 13-segment card browsed at page size 3 now reports 5 pages of which one carries a clip. Pinned by `tests/tier2/test_red_card_shapes.py::test_a_paged_scan_of_a_card_reports_files_per_page_not_clips`. |

| FR-36 | red-multi-component | 2026-09-01 | **(RED multi-component import — the COMPONENT BUDGET, defect A.)** `Clip._count_media_components` took `video` over `extra_files + [main_file]`, i.e. Codemill's `video=len(extraFileIds['video']) + 1`, which assumes the anchor contributes a video component. An anchor Vidispine cannot decode yields a `binaryComponent` instead: it satisfies the container slot (proved by the 10 single-segment drop-frame clips, which promote normally) and fills NO video slot, so one declared slot is never filled and the shape stays a placeholder for ever — no `original` tag, no transcode, no error. Signature on all 31 stuck clips of the 2026 STARLUX shoot (measured 2026-08-29..2026-09-01): `files == videoComponents + 1`, `containerComponent` absent, `binaryComponent` present; Codemill's own import path fails identically, reproduced through the Portal UI with no plugin involved, so a future "realignment" on their source silently reintroduces this. The anchor is now counted as a video contributor only when the PROVIDER says it deduces (`providers/providers.py::MAIN_FILE_YIELDS_VIDEO`, read by `yields_video_component`, default `True` — so `file`, `panasonicP2`, `xdcam`, `ikegami`, `hdslr`, `zoom`, `avchd` and `atomos` keep today's count exactly); `red` answers it off REDline's already-required, already-persisted `Abs TC` (`providers/red.py::anchor_yields_video_component`) — whole-field `HH.MM.SS.FF` means no video component, 110/110 on that shoot, and anything matching neither shape falls back to `True` with a WARNING. It is a BRANCH, never a constant: `video=len(extras)` for a deducible anchor makes its own video component overflow the budget and Vidispine answers `400 {"invalidInput": {"explanation": "No more components of that type is accepted", "value": "VIDEO_COMPONENT"}}` (measured 2026-08-31). `models/clip.py` learns nothing about codecs or timecodes. Pinned by `tests/tier1/test_red_segment_grouping.py::test_a_colon_separated_timecode_anchors_a_video_component`, `::test_a_dot_separated_timecode_anchors_no_video_component`, `::test_an_unreadable_timecode_keeps_the_pre_existing_declaration`, `::test_a_provider_that_never_declares_the_key_still_counts_its_anchor`, `::test_the_reader_coerces_whatever_a_provider_put_in_the_dict`, `::test_the_reader_survives_a_main_file_that_is_not_a_mapping` and `tests/tier2/test_ingest_discipline.py::test_a_deducible_anchor_declares_a_video_slot_of_its_own`, `::test_a_non_deducible_anchor_declares_one_video_slot_fewer`, `::test_a_non_deducible_anchor_with_an_audio_extra_declares_both`, `::test_the_anchors_own_audio_track_is_still_counted`, `::test_a_provider_dict_without_a_type_never_raises_in_the_count`, `::test_a_non_deducible_anchor_promotes_its_shape_end_to_end`, `::test_a_deducible_anchor_promotes_its_shape_end_to_end`. *(Superseded 2026-09-02, iteration 4 — D6: "falls back to `True` with a WARNING" is no longer what `red` does; an unreadable timecode is declared `None` and the multi-component import REFUSES to declare on it — the FR-36 row of 2026-09-02 below. Iteration 5: that refusal is made only on a run that would DECLARE, never on a resume into a shape that already holds a file.)* |

| FR-36 | red-multi-component | 2026-09-01 | **(RED multi-component import — the ANCHOR RACE, defect B. This makes a multi-component ingest BLOCKING where it was fire-and-forget.)** `doImportToPlaceholder` returns a job per extra component and the loop logged the id and dropped it, while the MAIN import — whose job is the only thing that evaluates the placeholder, creates the shape and starts the transcode — was issued immediately after. An anchor job that reaches that step first sees an incomplete placeholder, logs `Skipping transcode`, and nothing re-evaluates afterwards; on a 29-clip batch (2026-08-31) the only 2 failures were the only 2 clips whose anchor job finished before the last segment job (16:37:31 vs 16:37:51; 16:58:50 vs 16:59:13), and the job ends `FINISHED` when the race is lost against `FINISHED_WARNING` when it is won. Codemill's code carries the same race. The ids are now deduped, polled with BACKOFF (`EXTRA_COMPONENT_POLL_SECONDS` 2s growing to `EXTRA_COMPONENT_POLL_MAX_SECONDS` 15s, logging only when the pending set CHANGES — a wedged Vidispine is the case this loop runs for and must not be hammered), and the wait is satisfied only by a component that LANDED: every job at a TERMINAL status (there is no `waitForJob` on the 6.2.1 server, so this is a bounded poll) AND every file attached to the placeholder shape, because a terminal job may have ended `FAILED_TOTAL`/`ABORTED` and attached nothing, and treating that as success re-creates the very incomplete set this row removes. Terminal is read from `getStatus()`, NOT from `inProgress()` — see the status row below. A job that cannot be READ counts as still running, one rule with both arms (`getJob` raising AND `getJob` answering `None`). An expired wait is a FAILURE, not a fallback: the anchor is NOT imported and the clip is `failed`, because importing it anyway is exactly the race being removed — same verdict for an extra whose import answers without a `jobId`. **The bound is PER ENTRY POINT**: `EXTRA_COMPONENT_WAIT_SECONDS` 300s for the scan, `REST_EXTRA_COMPONENT_WAIT_SECONDS` 30s threaded from `views.py` through `Folder.ingest`/`Clip.ingest`/`Clip.import_file` — a cron holds nothing, a DRF request holds a thread and a database connection. Deliberately NOT a `RunOptions` field: it is a property of who is holding the caller, not of the run. Cost in the normal case is seconds (an already-hashed file attaching to a placeholder); cost against a wedged Vidispine is the bound, per clip. Pinned by `tests/tier2/test_ingest_discipline.py::test_the_anchor_waits_for_the_extras_and_then_proceeds`, `::test_a_component_job_that_stops_without_attaching_is_not_landed`, `::test_a_job_that_cannot_be_read_counts_as_still_running` (both arms), `::test_an_expired_wait_does_not_import_the_anchor`, `::test_an_extra_component_without_a_job_id_stops_the_anchor`, `::test_the_rest_entry_point_does_not_get_the_scans_patience`, `::test_the_rest_bound_is_shorter_than_the_scans`, `::test_the_anchor_is_deferred_until_the_extras_have_landed` and `::test_an_expired_wait_leaves_a_failed_clip_and_a_reusable_placeholder`, `::test_the_poll_dedupes_job_ids_and_logs_only_when_the_set_changes`. *(2026-09-02: three unpinned seams closed.)* The shape is re-read only when the pending set MOVES — nothing but a job finishing attaches a file, and the poll backs off where the shape read did not, so an un-gated read spent one query per couple of seconds of a 300 s wait hammering the wedged Vidispine this loop runs for (`::test_the_wait_re_reads_the_shape_only_when_the_pending_set_moves`). The two arms of "never attached their file" — "although every import job has stopped" versus "and no import job was running for them" — send the operator to two different places and were invertible with the suite green (`::test_the_wait_says_which_of_the_two_unlanded_states_it_is_in`). And an UNREADABLE shape is a FIFTH reason of its own, not the fourth: whether the components attached is unknown, so saying they never did would name a cause the run never observed (`::test_the_wait_names_an_unreadable_shape_rather_than_guessing`, and USER_GUIDE.md's Troubleshooting now lists it). `::test_components_are_declared_then_imported_before_the_main_file` still pins Codemill's ORDER, unchanged. |

| FR-35 | red-multi-component | 2026-09-01 | **(RED multi-component import — the three placeholder states, and a failure that leaves a RESUMABLE item.)** `_get_or_create_placeholder_shape` treated every non-empty placeholder as one undifferentiated anomaly: `log.info("Shape is not a placeholder")` + `return None`, which `import_file` turned into a bare `result["failed"] = True`. It now returns a `PlaceholderShape(shape_id, attached_file_ids)` and distinguishes three states. (1) No placeholder — create one, unchanged. (2) An EMPTY placeholder — reused, silently, which is the FR-36 incomplete-import retry rung, unchanged. (3) A placeholder HOLDING FILES, split in two: INCOMPLETE (some of this clip's file ids are missing from the shape) is RESUMED — the missing components are imported, the already-attached ones are skipped so they cannot duplicate a component or overflow the budget, then the anchor, and the item promotes; COMPLETE and still a placeholder is the one dead end, reported through the ERROR channel naming the shape, every file attached and the component set it was told to expect. This is a real behavioural widening: a clip whose previous import was interrupted mid-way used to be `failed` for ever and is now finished on the next run, so a transient Vidispine slowdown no longer converts into permanent manual work. The message deliberately does NOT suggest `--replace`: its shape query runs `placeholder=False`, so a stuck item exposes no shape to remove and the advice could not be followed. The FR-35 skip on a REAL (non-placeholder) `original` shape holding files is decided a rung earlier in `import_file` and is untouched. Pinned by `tests/tier2/test_ingest_discipline.py::test_an_empty_placeholder_is_still_reused_silently`, `::test_a_partially_filled_placeholder_is_resumed_not_condemned`, `::test_a_complete_but_unpromotable_placeholder_reports_through_the_error_channel`, `::test_an_already_attached_component_is_not_imported_twice`, `::test_the_next_run_resumes_a_partially_imported_placeholder` and `::test_a_complete_unpromotable_placeholder_reaches_the_run_summary`. |

| FR-22 | red-multi-component | 2026-09-01 | **(RED multi-component import — the failure reaches the operator's report.)** `models/folder.py`'s ingest loop appended to `response["errors"]` ONLY in its `except` arm, so a clip that came back `{"failed": True}` without raising contributed nothing to it: the run printed "31 failed, 0 errors" and the operator's report said nothing about why — the exact silence this story is named for. `Clip.error` was not the channel either: it is in-memory and has NO production reader (`serializers.py:84` points at a non-existent `get_error`, pre-existing and deliberately untouched here). A failed clip that RECORDED a reason now appends `Failed to ingest clip {clip}: {reason}` to `response["errors"]`, which is what `_folder_worker` counts, what the per-folder log line prints and what `scan/coordinator.py::summary_lines` totals — so both the folder line and the run summary's error count move off zero. Deliberately GATED on a recorded reason: the pre-existing FR-36 single-component "import response carried no job id" rung keeps its 2.5 behaviour (counted `failed`, not in `errors`) rather than adding a report line that names no cause, which is why `::test_a_real_import_without_a_job_id_is_failed_never_ingested`'s `response["errors"] == []` still passes unchanged. Pinned by `tests/tier2/test_ingest_discipline.py::test_an_expired_wait_leaves_a_failed_clip_and_a_reusable_placeholder`, `::test_a_complete_unpromotable_placeholder_reaches_the_run_summary` and `::test_the_next_run_resumes_a_partially_imported_placeholder`. |

| FR-4 | red-multi-component | 2026-09-01 | **(Test-double delta — no production behaviour.)** `tests/portal_stub` now models the COMPONENT BUDGET, without which neither defect above is observable at all: `updatePlaceholderComponentCount` stores the expectation on the shape (and raises `UnknownShape` when it names a shape the fake never created, where it used to be a silent no-op); imports CLAIM slots and are refused over budget with `ComponentBudgetExceeded`, carrying the measured `400 … VIDEO_COMPONENT`; a container import lands in a BINARY slot when a fixture calls `VidispineFake.set_non_deducible(file_id)`; files are attached when the JOB LANDS rather than at request time (`VidispineFake.hold_jobs` / `stall_jobs`, so the race is reproducible with no threads and no clock); and promotion happens only when the ANCHOR's import lands on a satisfied budget, replacing the unconditional promotion on any `jobId` and the hardcoded `VX-IMPORTED-FILE`. Slots are claimed only once the import is known to proceed — after the queued response and after the fault point — so a call that started no job leaves the slot free for the resume. All new class state is mutated under the existing `_lock` (story 3.2) and cleared by `reset()`. Pinned in its own right by `tests/tier2/test_ingest_discipline.py::test_the_stub_promotes_only_when_every_declared_slot_is_filled`, `::test_the_stub_promotes_when_the_anchor_lands_on_a_full_budget`, `::test_the_stub_refuses_a_component_over_the_declared_budget`, `::test_the_stub_lands_a_non_deducible_anchor_in_a_binary_slot`, `::test_the_stub_refuses_a_budget_declared_against_an_unknown_shape` and `::test_an_import_that_starts_no_job_claims_no_slot`. `::test_multi_component_import_without_a_job_id_is_not_an_ingest` keeps its assertion and its intent; only its local double was re-baselined, so its extra component answers WITH a job id — otherwise the wait short-circuits before the container import that pin exists to check. |

| FR-35 | red-multi-component | 2026-09-01 | **(RED multi-component import — the RESUME accounts for jobs still IN FLIGHT, not only for files already attached.)** Review iteration 2's first finding. The resume rung above compares the shape's ATTACHED files against the clip's expected ones, and a run that starts while the PREVIOUS run's component jobs are still running sees those files missing — the job has not attached them yet — and re-imports them. A component slot is consumed when a component LANDS, not when the import is requested (measured on prod 2026-09-01 with a CONTROL arm: two imports fired 117 ms apart both succeed, the second is refused once the first has attached), so the duplicate lands too and earns `400 {"invalidInput": {"explanation": "No more components of that type is accepted", "value": "VIDEO_COMPONENT"}}` — turning the resumable case into the hard failure the resume exists to avoid. `Clip._in_flight_component_files` therefore reads the item's `PLACEHOLDER_IMPORT` jobs through `JobHelper.getAllJobsForItem` (already used by `Clip.jobs`), keeps those that have NOT reached a terminal status (`getStatus()`, not `inProgress()` — see the status row below), and identifies which component each one is importing **by its SOURCE PATH**. Not by job data: MEASURED on the 6.2.1 server 2026-09-01 on two real `PLACEHOLDER_IMPORT` jobs of one multi-component RED clip (VX-696013, an extra component, and VX-696024, the anchor, both on item VX-216268), the whole of their `data` is `item` for the extra and `errorMessage`, `item`, `transcodeProgress`, `transcodeWallTime` for the anchor — there is **no `sourceFileId` and no `fileIds`**. Those two keys appear in the vendor's own test fixture (`portal/vidispine/tests/test_items.py`) and a first version of this code read them, which would have answered "nothing is in flight" on this server for ever, silently degrading to the re-import the skip exists to prevent. What the job object does expose is the accessor family `get_related_jobs` already uses: `getSourceFilePath()` → `file:///mnt/PAD_Storage/AA%20-%20RUSHES%20TAPELESS/2026/.../K001_K003_0804O6_002.R3D`, `getFilename()` → `K001_K003_0804O6_002.R3D`, `getTargetItem()` → `VX-216268`. That value is a **percent-encoded `file://` URI** and the production storage root contains spaces (`AA - RUSHES TAPELESS`), so it is unquoted, stripped of its scheme and normalised before comparison — a comparison against the raw URI matches nothing and fails *silently*, the same class of bug as reading the wrong key. Matching is then three rules in order, because `getSourceFilePath()` is ABSOLUTE under the storage root while a provider's `path` is storage-RELATIVE: exact match, component-ALIGNED suffix (`endswith(os.sep + path)`, so `.../xa_002.R3D` never satisfies `a_002.R3D`), then equal basenames — the last safe only because the jobs are already filtered to this ITEM (and a job naming another item is skipped) and the candidates to this CLIP's own media files, two of which never share a basename. A file with a running job is NOT re-imported and its job IS waited for. Unlike the WAIT's unreadable-job rule, an unreadable listing, an unreadable job, a job with no readable source and a source matching nothing all answer "nothing known" and fall back to today's behaviour: the cost is at worst the pre-existing 400 — loud, reported and recoverable — where refusing would make one failed listing permanently unresumable. The listing is bought only when the placeholder was REUSED (`PlaceholderShape.created` is False): a shape this run minted cannot carry an earlier run's jobs, so the happy path keeps exactly the Vidispine calls it made before. Pinned by `tests/tier2/test_ingest_discipline.py::test_a_running_import_job_is_identified_by_its_source_path`, `::test_the_percent_encoding_is_what_makes_the_naive_comparison_fail`, `::test_a_job_that_has_stopped_is_not_in_flight`, `::test_the_suffix_match_is_component_aligned`, `::test_the_basename_is_the_last_resort_when_the_roots_disagree`, `::test_a_job_targeting_another_item_is_never_counted`, `::test_an_unreadable_job_listing_leaves_the_resume_on_attached_files` (four arms), `::test_a_job_object_without_the_accessors_is_not_in_flight`, `::test_a_provider_path_containing_a_percent_sequence_is_not_unquoted` (only the JOB side is a URI: unquoting the provider's own `VSFile.getPath()` would rewrite a real filename containing a `%` sequence), `::test_a_component_whose_job_is_still_in_flight_is_not_re_imported`, `::test_the_in_flight_job_is_waited_for_like_any_other`, `::test_a_fresh_placeholder_buys_no_job_listing`, `::test_a_resume_racing_an_in_flight_job_finishes_on_the_run_after` and `::test_a_created_placeholder_says_it_was_created`. |

| FR-35 | red-multi-component | 2026-09-01 | **(RED multi-component import — a placeholder holding a FOREIGN file is refused, never resumed.)** The resume rung reads `expected - attached`; `attached - expected` is a different condition entirely and used to be invisible. A placeholder holding a file that is not this clip's is not a partially finished import of this clip at all, and importing into it would attach this clip's media to another clip's item. It is now refused with `shape_id=None` and reported through the error channel, naming the foreign file ids and asking which item they belong to — deliberately NOT suggesting `--replace`, whose shape query runs `placeholder=False` so a stuck item exposes no shape to remove. The rung is GATED on the caller having said what it expected: with no media files supplied, `expected` is empty and a non-empty placeholder keeps the pre-story dead-end verdict rather than being mislabelled foreign. Pinned by `tests/tier2/test_ingest_discipline.py::test_a_placeholder_holding_a_foreign_file_is_refused_not_resumed` and `::test_a_caller_that_names_no_files_still_gets_the_dead_end_verdict`. |

| FR-36 | red-multi-component | 2026-09-01 | **(RED multi-component import — the WHOLE-REQUEST budget on the REST path, and the bound as a parameter rather than folder state.)** Review iteration 2's second finding. The short REST bound is per CLIP, and neither REST branch ingests one clip: `views.py`'s `__all__` branch runs a whole folder through `Folder.ingest` and its other branch loops over the request body, so a 50-clip folder against a wedged Vidispine held a DRF request thread and its database connection for 50 x `REST_EXTRA_COMPONENT_WAIT_SECONDS`. `views.py` now opens a `REST_COMPONENT_WAIT_BUDGET_SECONDS` (60 s) deadline before touching any clip and passes it beside the per-clip bound; `_import_multi_component` clamps its bound to `min(bound, deadline - now)` and the poll's backoff is clamped to the same remainder, so no sleep overshoots the budget. The message says when the clamp is what expired. Both values travel as EXPLICIT parameters the whole way — `views.py` → `Folder.ingest` → `_folder_worker` → `_ingest_pass` → `Clip.ingest` → `Clip.import_file` → `_import_multi_component` — and NOT as folder state: with `self._component_wait_seconds` + `getattr(self, ..., None)`, deleting a hop is a silent `None` (the cron's five-minute patience, per clip, on a held request thread) rather than a signature change a test can watch. They are not `RunOptions` fields either: they are a property of who is holding the caller, not of the run. Pinned by `tests/tier2/test_ingest_discipline.py::test_the_whole_request_budget_clamps_the_per_clip_bound`, `::test_a_deadline_further_out_than_the_bound_does_not_shorten_it`, `::test_the_rest_request_budget_is_bigger_than_one_clips_bound`, `::test_the_rest_folder_branch_passes_the_short_bound_and_the_budget`, `::test_the_rest_clip_branch_passes_the_short_bound_and_a_live_deadline`, `::test_the_component_wait_deadline_opens_after_the_scan_pass`, `::test_folder_ingest_hands_the_entry_points_bound_to_every_clip`, `::test_folder_ingest_hands_the_whole_request_budget_to_every_clip`, `::test_the_default_bound_is_the_scans` and `::test_the_poll_interval_stops_at_the_ceiling`. |

| FR-22 | red-multi-component | 2026-09-01 | **(RED multi-component import — the report is CAPPED, the anchor's own rung reaches it, and a reason never outlives its attempt.)** Three corrections to the error channel above. (1) `models/folder.py::INGEST_ERROR_REPORT_CAP` (10) bounds how many per-clip reasons one folder puts into `response["errors"]`: the wedged Vidispine that fails one clip fails every clip of the card the same way, and one line each would bury the rest of the run's report — and the Slack message built from it — under N identical lines. Past the cap a closing line reports how many were not listed, so the report is short but never silently short. (2) The ANCHOR's own job-less response — the extras all landed and `doImportToPlaceholder` answered without a `jobId` — now records its reason on `Clip.error`, so it reaches the report like every other new failure instead of printing "1 failed, 0 errors". (3) `Clip.import_file` RESETS `self.error` as its first statement: one clip object can be imported twice in a process (the retry rung, a two-run test), and `Folder.ingest` reads that attribute to decide what the operator is told, so a previous attempt's reason must not be attributed to this one. Pinned by `tests/tier2/test_ingest_discipline.py::test_the_error_channel_is_capped_but_says_it_was_capped`, `::test_the_anchors_own_job_less_response_reaches_the_run_summary` and `::test_a_second_import_does_not_inherit_the_first_ones_reason`. |

| FR-36 | red-multi-component | 2026-09-01 | **(RED multi-component import — the landing check tells PROMOTED apart from UNREADABLE.)** The wait's landing check read `getItemShapesFromNames(..., placeholder=True)` and treated an empty answer as "could not tell", i.e. not landed. But `placeholder` is a three-state FILTER on that endpoint, not an include flag (`ItemAPIEnhanced.getItemShapeIdsFromNames` sends `placeholder=true` or nothing, and nothing means "non-placeholder shapes only"), so an item whose shape has been PROMOTED answers the first query with nothing while holding every one of its files — the opposite of unknown. The check now falls back to the non-placeholder query when the first is empty, and only "neither" is unreadable. The second query is bought only in the case where the first came back empty. Pinned by `tests/tier2/test_ingest_discipline.py::test_the_landing_check_reads_the_placeholder_shapes_files`, `::test_a_promoted_shape_is_landed_not_unreadable`, `::test_no_shape_at_all_is_unreadable` and `::test_a_shape_read_that_raises_is_unreadable`. |

| FR-4 | red-multi-component | 2026-09-01 | **(Test-double delta — no production behaviour.)** Two additions to the stub's component budget. (1) `JobHelperFake.getAllJobsForItem` takes the real signature (`item_id, job_type=None, max_hits=0`) and answers the item's registered component jobs as `FakeJob`s, which now carry `getSourceFilePath()`, `getFilename()` and `getTargetItem()` — the accessors production actually exposes. `getDataByKey`/`sourceFileId` modelling was REMOVED: the keys are measured absent on the 6.2.1 server (see the FR-35 row above), so a fake answering them would certify a lookup that can never work. `VidispineFake.set_file_path(file_id, path)` is how a test tells the fake the path Vidispine knows a file by — knowledge the fake has and the plugin does not, since the import only ever sends a `fileId`, which is exactly the asymmetry that makes the source path the only identifier. The URI is built under `JOB_SOURCE_ROOT = "/mnt/PAD_Storage/AA - RUSHES TAPELESS"` and percent-encoded, deliberately carrying SPACES: a space-free root could not fail a missing `unquote`. Without all of this the resume path's second input does not exist in the suite and "a component whose job is still in flight is not re-imported" could only be pinned at the seam. (2) `declare_component_budget` documents and holds the MEASURED re-declaration semantics: re-declaring REPLACES the declaration and gives NO consumed slot back, so `budget` is overwritten while `claimed`/`landed` are deliberately untouched. That was measured on prod 2026-09-01 with a control arm (control, no re-declaration: A lands, B refused with the exact 400; measured, with re-declaration: B refused too) after a first attempt concluded "accumulates" and was WRONG — its two imports were 117 ms apart with job A still `READY`, so it measured latency, not the budget. The whole resume path depends on it, since a resume re-declares before importing what is missing. Pinned by `tests/tier2/test_ingest_discipline.py::test_the_stub_lists_an_items_component_jobs`, `::test_re_declaring_the_budget_replaces_it_and_frees_nothing`, `::test_an_undeclared_budget_refuses_nothing` (an undeclared budget names the container slot and nothing else, so a shape nobody declared one for refuses nothing — a default of `video: 0` had the fake refuse a single-component import for a rule production never applies to it) and `::test_an_audio_anchor_fills_an_audio_slot_not_a_video_one` (`set_audio_anchor`: a P2 anchor can BE an audio track, and `_count_media_components` budgets it as one, so the fake has to fill an audio slot from a container import or that branch could be reverted with the suite green). The fake's `_lock` is now an `RLock`: the budget helpers nest (`claim_component_slots` -> `_budget`, `promote_if_complete` -> `_budget`/`slots_for`) and every one of them has to be guarded, which a plain `Lock` made a deadlock and which is why the inner ones were unguarded despite this row's own claim. *(2026-09-02.)* `claim_component_slots` still opened its critical section one statement too LATE — the `_budget` read (a `setdefault`, so a write) sat outside it, leaving read-budget/check-claimed/increment non-atomic, which is the whole point of the section; the lock now opens before the read. `IngestHelperFake.importFileToPlaceholder` no longer attaches the file inline: it claims the slots and registers a container JOB, the same two phases as `doImportToPlaceholder`, so the request/landing gap the single-component resume lives in exists in the fake. An unheld job settles inside `register_component_job` and lands the file exactly as before, so every existing fixture sees the state it always did; the claim and the registration stay BEFORE this call's fault point, because its fault models a process that died with the Vidispine write already committed. That lock-ordering change is a RACE: it is not pinnable deterministically (the mutation — opening the lock after the `_budget` read — was applied and left the suite green, 0 failed), so it is asserted by inspection of the critical section, not by a test. *(2026-09-02, second pass.)* Four more stub deltas. (a) The default import response is no longer the ONE constant `{"jobId": "VX-JOB-DEFAULT"}`: every import registers a job under its id, so two pool workers importing single-component clips with no queued response both registered under the same key and worker B's entry overwrote worker A's before A settled it — A's settle landed B's file and A's placeholder never promoted; the fake now mints `VX-JOB-DEFAULT-<n>` per default answer, and `register_component_job` settles INSIDE its lock (the settle-outside-lock half is again a race — mutation applied, 0 failed — asserted by inspection). Pinned by `tests/tier2/test_coordinator_tree.py::test_a_pooled_run_registers_one_distinct_import_job_per_clip` (the constant id reddens it deterministically: one `component_jobs` entry for three ingested clips). (b) `claim_component_slots` refuses a file already attached to ANY shape of the item, promoted or not, with the measured `400 … VIDEO_COMPONENT` text: the budget check only looked at a still-placeholder shape, so a landing that PROMOTED the shape made the fake blind to a duplicate container import — the request the plugin's resume guards exist never to send was silently accepted. Pinned by `tests/tier2/test_ingest_discipline.py::test_the_stub_refuses_a_duplicate_import_into_a_promoted_shape`. (c) Both import entry points raise `ImportWithoutAFileId` for a `None` file id — `providers/file.py` builds `{"file_id": None}` for a clip with no file, and a fake accepting it certified a request Vidispine would 400 on. Pinned by `::test_the_stub_refuses_an_import_with_no_file_id`. (d) `hold_jobs` arguments are STICKY within a test (`settle_after_polls` changes only when named, via a `KEEP` sentinel; `containers=True` is never turned back off), and `stall_jobs` lost its `containers` parameter — a named stalled job is stalled whatever its component, so the flag had no caller and could not have changed any outcome. Pinned by `::test_hold_jobs_arguments_are_sticky_within_a_test`. *(Superseded 2026-09-02, iterations 4 and 5: `claim_component_slots` is now `refuse_if_budget_full` and checks at REQUEST against LANDED files — the FR-4 row of 2026-09-02 below; and (b)'s attached-file scan no longer quotes the 400, which was never measured for a re-import of the same FILE — it trips `DuplicateImportOfAnAttachedFile`, an UNMEASURED tripwire, see the FR-4 row of iteration 5.)* |

| FR-36 | red-multi-component | 2026-09-01 | **(RED multi-component import — `inProgress()` is not "has it stopped"; the STATUS decides.)** Verified in the vendor bytecode on the 6.2.1 server: `VSJob.inProgress()` answers True for `STARTED`, `READY` and `STARTED_ASYNCHRONOUS` and False for everything else — including **`WAITING`**, which `VSJob.STATUSES` declares and which is an ordinary status on a busy Vidispine, i.e. exactly the condition the wait exists for. Reading it as "stopped" broke both halves of this story: `_wait_for_components_to_land` emptied its pending set on the first pass and concluded "never attached although every import job has stopped" after two confirmations instead of using its bound, so a clip whose imports were merely QUEUED was failed; and `_in_flight_component_files` missed the job, so the resume re-imported a component whose import was already queued — landing it twice and earning the measured 400. Both now read `Clip._job_has_stopped`, which maps `getStatus()` through `JOB_STATUSES_TERMINAL` (`FINISHED`, `FINISHED_WARNING`, `FAILED_TOTAL`, `FAILED`, `ABORTED`) and `JOB_STATUSES_STILL_COMING` (`READY`, `STARTED`, `STARTED_ASYNCHRONOUS`, `WAITING`); `inProgress()` is demoted to the FALLBACK for a job object that reports no status at all. A status NEITHER set knows answers `None` — "cannot tell" — and the two callers resolve that in OPPOSITE directions on purpose: the wait treats it as still running (closing a component set on an unknown outcome is what manufactures the unpromotable placeholder), the resume treats it as not in flight (refusing to import on an unknown would make one bad read permanently unresumable). The LOCAL doubles in `test_ingest_discipline.py` (`_JobFake`, `_ListedJobFake`) gained an optional status so the mapping is actually EXECUTED — before, `_job_status` answered `None` every time and the whole mapping could be reverted to the measured-wrong reading with the suite green; mutating `_job_has_stopped` reddens 14 tests through them, so the mapping itself is genuinely pinned. *(2026-09-02: narrowed.)* The claim did NOT hold for the STUB. `VidispineFake.set_job_status`, `VidispineFake.job_status` and `FakeJob.getStatus` had no test caller at all, so the stub always answered `None` and every stub-driven end-to-end test exercised the `inProgress()` FALLBACK alone. The stub's status layer is now load-bearing in exactly one place: `::test_a_one_file_clip_does_not_start_a_second_container_import` puts a held container job into the measured contradiction (`getStatus()` = `WAITING`, `inProgress()` = False) and the resume can only see that job by READING THE STATUS — stubbing `FakeJob.getStatus` back to `None` reddens it. Pinned by `tests/tier2/test_ingest_discipline.py::test_a_waiting_job_is_still_coming_although_inprogress_says_no`, `::test_a_terminal_status_stops_the_job`, `::test_a_still_coming_status_keeps_the_job`, `::test_an_unmodelled_status_is_neither`, `::test_a_job_with_no_status_falls_back_to_inprogress`, `::test_the_wait_keeps_polling_a_waiting_job`, `::test_a_waiting_job_is_counted_in_flight_by_the_resume` and `::test_an_unmodelled_status_is_running_for_the_wait_and_absent_for_the_resume`. |

| FR-35 | red-multi-component | 2026-09-01 | **(RED multi-component import — the ANCHOR is subject to both resume guards, and so is the single-component path.)** `attached_file_ids` and `in_flight` were consulted only inside the `for extra_file in extra_files` loop, so the anchor import was UNCONDITIONAL: a placeholder whose container import had landed — an interrupted run whose anchor went first — was classified incomplete, resumed, and the anchor imported a second time, which is the duplicate component and the measured 400 the resume exists to avoid. Three changes. (1) `_get_or_create_placeholder_shape` now calls an ATTACHED ANCHOR a dead end whatever else is missing: the anchor's job is the only thing that evaluates a placeholder and nothing re-evaluates one, so no re-run can promote the item — the message names the shape, the components that never attached, and the manual removal. (2) `_import_multi_component` re-states the same refusal where the import happens, deliberately redundant because the method is also called directly. (3) An anchor whose own import job is STILL RUNNING is waited for instead of re-imported, and its job id is what the row records. *(2026-09-02)* Recording it is now GUARDED. The `job` setter does `self.job_id = job.getId()` and only the GETTER ever caught `NotFoundError`, so a purged job — or a `getJob` answering `None` — raised `AttributeError` on the SUCCESS path of the resume, in the branch that had just correctly decided not to start a second import. Both resume sites go through `Clip._record_job`, which writes the id FIRST and fetches the object after: an unfetchable job costs a warning line, not the clip's ingest, and never the id — losing the id leaves the row job-less, which the `retry_incomplete` rung reads as "never imported" and brings straight back to fire the import this branch refused to send (`::test_the_recorded_job_id_survives_a_job_that_cannot_be_fetched`, all three arms, each arm's log wording pinned — the `NotFoundError` arm says "is not known to Vidispine", the broad arm "could not be read", and deleting the former reddens its arm). *(2026-09-02, second pass.)* The two FRESH import sites — the single path after `importFileToPlaceholder`, the multi path after the anchor's `doImportToPlaceholder` — did `self.job = job_helper.getJob(job_id)` raw, which is WORSE than the resume case: the import had already been SENT, so an `AttributeError` there left a job-less row with a real container import running, and `retry_incomplete` fired a duplicate next run. Both go through `_record_job` now (`::test_a_fresh_single_import_keeps_its_job_id_when_the_job_cannot_be_read`, `::test_a_fresh_anchor_import_keeps_its_job_id_when_the_job_cannot_be_read`). `_record_job` also writes its cache on EVERY arm (`None` on the failing ones) and assigns the object directly rather than through the `job` setter: the getter is `hasattr`-gated and refetches through a fresh helper catching only `NotFoundError`, so a swallowed `RuntimeError` re-raised on the next `clip.job` read (the serializer, on the REST path), a second `_record_job` on the same instance kept the FIRST call's object under the second call's id, and the setter's `self.job_id = job.getId()` would silently replace the id as sent (`::test_a_swallowed_job_read_failure_does_not_come_back_on_the_next_read`, `::test_a_second_record_does_not_keep_the_first_calls_job_object`, `::test_the_recorded_job_id_is_the_one_sent_not_the_one_read_back`). The single path additionally refuses an anchor with no file id BEFORE sending, as the multi path already did (`::test_a_single_component_import_without_a_file_id_is_refused_before_sending`). The SINGLE-component path was outside both guards entirely — `_in_flight_component_files` was computed only in the multi branch — and it is reachable: an interrupted import leaves the placeholder still EMPTY while its job runs, so the empty placeholder is reused silently and the `retry_incomplete` rung brings the next scan back to fire a second container import. The lookup is now computed once for BOTH branches, with the anchor in the media-file list. (4) *(2026-09-02)* The refusal in (2) is taken BEFORE the declaration and before the extras loop. Stated after the loop — where it was first written — the method declared the component count and sent every missing extra import into a shape it was about to call a dead end; those components LAND, and a landed component consumes a slot on a placeholder nothing can ever promote. The verdict was right and the harm was already done. Making this observable required TWO load-bearing stub changes, and until 2026-09-02 neither was complete: `IngestHelperFake.importFileToPlaceholder` attached the file INLINE instead of through a job — so the single-component path's container import could not be put in flight at all — and `hold_jobs(containers=True)` ASSIGNED `hold_container_jobs` rather than setting it stickily, so a later `hold_jobs()` silently un-held the container job (`register_component_job` had already stopped forcing `settle_now` for containers, which is a precondition, not a change a test can observe on its own). `stall_jobs(containers=True)` is STRUCK: the parameter had no caller and could not change the outcome for the job it named — a named stalled job is stalled whatever its component — so it was removed rather than given a caller. The stickiness itself is pinned by `::test_hold_jobs_arguments_are_sticky_within_a_test` (see the FR-4 stub row). Pinned by `tests/tier2/test_ingest_discipline.py::test_the_anchor_is_not_re_imported_when_it_is_already_attached`, `::test_the_classifier_calls_an_attached_anchor_a_dead_end`, `::test_an_attached_anchor_is_refused_before_any_extra_is_imported` (the placement), `::test_an_anchor_whose_job_is_still_running_is_waited_for_not_re_imported`, `::test_every_running_job_for_one_file_is_waited_for`, `::test_two_running_anchor_jobs_are_both_waited_for_and_the_first_is_recorded` (two running imports of the anchor: all waited for, `anchor_jobs[0]` recorded, and a warning names the count and which one) and — for the SINGLE-component path — `::test_a_one_file_clip_does_not_start_a_second_container_import`, which drives the real `import_file` end to end. The anchor-no-job failure message now counts COMPONENTS covered by running jobs and lists the JOB ids apart (`::test_one_component_job_is_counted_once_in_the_failure_message`); the hoisted anchor refusal guards a `None` `shape_id` in its message and appends the component-budget report, and the classifier's FOREIGN-file rung (`attached - expected`) is re-stated in `_import_multi_component` for the same direct-call reason (`::test_the_anchor_refusal_names_the_shape_it_can_and_reports_the_budget`, `::test_a_direct_call_with_a_foreign_file_on_the_shape_is_refused`). `import_file` hands `_in_flight_component_files` `importable_extras(extra_files) + [main_file]` — the SAME filter the import loop and `_expected_file_ids` use — so an `xdcam`-style `metadatas` extra whose basename matches a running job is never read as one of this clip's in-flight components (`::test_a_non_importable_extra_is_never_read_as_in_flight`). `::test_a_single_component_clip_does_not_start_a_second_import` pins only the HELPER's refusal when it is HANDED a running job: it calls `_import_single_component` directly with a hard-coded `in_flight_jobs`, so it never reaches the code in `import_file` that COMPUTES `in_flight` and passes it, and deleting that argument at the call site left the suite green until 2026-09-02. |

| FR-36 | red-multi-component | 2026-09-01 | **(RED multi-component import — one component filter, and a media file with no id is a FAILURE.)** `_expected_file_ids` took every extra while the import loop skipped anything whose `type` was not `audio`/`video`. `providers/xdcam.py` really does build `{"type": "metadatas", "track": 1, "order": 1, "path": …, "file_id": <real id>}` for its sidecar XML, so a file id landed in `expected` that no import would ever send: `missing` was non-empty for ever, the shape never reached the dead-end verdict, and every run re-entered the resume and re-imported the anchor. Both sides now derive from `models/clip.py::importable_extras`, which filters on the TYPE alone — a non-media type is a legitimate skip, since no import sends it and `ignore_sidecars=True` keeps Vidispine from attaching it, so it can never be on the shape. A media-typed extra with NO `file_id` is the opposite case and is now REPORTED rather than filtered: excluding it made it budget nothing, expect nothing, import nothing and report nothing, and the clip came back INGESTED with a span file silently missing from the item. `_import_multi_component` refuses before sending anything, naming the paths. An anchor with no `file_id` is refused the same way (it would otherwise send `{"fileId": None}`, and it is what made the item's own anchor look FOREIGN to the resume, since `_expected_file_ids` cannot name it). Pinned by `tests/tier2/test_ingest_discipline.py::test_a_non_media_extra_is_neither_budgeted_nor_expected`, `::test_a_shape_holding_only_the_importable_files_is_the_dead_end` and `::test_a_media_extra_with_no_file_id_is_reported_not_filtered`. |

| FR-36 | red-multi-component | 2026-09-01 | **(RED multi-component import — a RESUME does not re-declare the component budget.)** Re-declaring REPLACES the declaration and gives no consumed slot back (measured 2026-09-01, with a control arm). Nothing kept the declaration STABLE across runs: `yields_video_component` is re-derived from `metadatas["timecode"]` on every run, a re-extraction rewrites that row, and an unreadable value takes the `True` fallback — so a later run could compute a DIFFERENT budget and overwrite the one the already-claimed slots were claimed against, producing exactly the permanently unpromotable placeholder this story exists to remove. `_import_multi_component` therefore declares only when NO landed file has consumed a slot against an earlier declaration: on a resume into a shape that holds files it logs what it WOULD have declared and leaves the declaration alone. *(2026-09-02: corrected, twice.)* The MEASURED rule, stated once: (1) a component slot is consumed when the file LANDS, not at request; (2) re-declaring REPLACES and frees nothing, so "a resume may safely re-declare"; (3) declared slots are not readable from the shape document. Consequence: an IN-FLIGHT job has consumed NOTHING, so a declaration nothing has been consumed against costs nothing to replace — and only an ATTACHED file implies a consumed slot. The gate is `bool(attached_file_ids)` alone. An intermediate version gated on `attached_file_ids or in_flight_component_files`, reasoning that a running job's slots were "claimed" — a reasoning error with a hard consequence: the single-component path never declares, so a run that took it (no extras visible yet) and left its container job in flight made the next run, now seeing the extras, SKIP the declaration and import them into a shape whose budget was never set. A `created` flag was considered and rejected: `_import_multi_component` is also called directly (the FR-35 row's "deliberately redundant" refusal), so the flag would be a fourth resume parameter every direct caller must remember to pass. The former RESIDUAL — "a job listing that fails answers nothing in flight, so such a resume can still re-declare" — is MOOT: in-flight jobs no longer gate the declaration, and re-declaring is free by (2). Pinned by `tests/tier2/test_ingest_discipline.py::test_a_resume_does_not_re_declare_the_component_budget`, `::test_a_resume_whose_jobs_are_still_in_flight_re_declares` (the inverted pin: nothing attached, one running job, declared as a first run would), `::test_a_run_that_first_saw_no_extras_declares_when_the_next_run_sees_them` (the single-to-multi transition through the real `import_file`: run 1 takes the single path with its container job held, run 2 sees the extras and `updatePlaceholderComponentCount` IS called with `container=1, video=2`) and `::test_a_first_run_still_declares_the_budget`. |

| FR-36 | red-multi-component | 2026-09-01 | **(RED multi-component import — the poll's two sleeps, the shape read, and the landing confirmations.)** Four corrections to `_wait_for_components_to_land`, each previously unobservable. (1) The poll sleep is CLAMPED to what is left of the bound — a 15 s ceiling on a 30 s REST bound overshoots the deadline by most of a poll, and on that path the overshoot is a request thread and its database connection held past the budget. (2) The CONFIRMATION floor (`LANDING_CONFIRMATION_SECONDS`, 1 s) is deliberately NOT clamped: past the deadline the poll clamp is 0, so the two `LANDING_CONFIRMATIONS` would run back to back and absorb none of the attachment race they exist for; the overshoot is bounded by `LANDING_CONFIRMATIONS x` the floor. (3) The placeholder shape is re-read only when the pending set SHRANK or emptied, never once per pass — nothing but a job finishing attaches a file, and the poll backs off while the shape read did not, so a five-minute wait spent one shape query every couple of seconds on an answer it already had. (4) The "never attached although every import job has stopped" message no longer claims a cause the run did not observe: an unreadable shape says so, and a wait with no component jobs at all says that instead. Pinned by `tests/tier2/test_ingest_discipline.py::test_the_poll_sleep_never_overshoots_the_remaining_bound`, `::test_the_confirmation_floor_is_not_clamped_away_by_an_expired_bound`, `::test_a_file_that_lands_between_two_reads_is_still_a_success` and `::test_the_poll_dedupes_job_ids_and_logs_only_when_the_set_changes`. |

| FR-36 | red-multi-component | 2026-09-01 | **(`views.py` / `serializers.py` — the REST endpoint could not succeed for ANY input, and now says why.)** Found by the first request-level test ever written against `ClipsInPathsView`, and fixed because neither ingest branch can run without it. (1) `ClipSerializer.is_valid` called `super().is_valid(raise_exception)` POSITIONALLY at three sites; DRF 3.16's `is_valid` is keyword-only, so every call through the override raised `TypeError` before reaching validation. (2) An INVALID folder body fell straight through the `if folder_serializer.is_valid():` with `folder` unbound, and the endpoint answered **500** on an `UnboundLocalError` for what was the client's own payload; it is now a **400** carrying `folder_serializer.errors`, which names the offending fields. (3) `FolderSerializer` declares `error` — required, and with no `Folder` model field behind it — so `Folder(**validated_data)` raised `TypeError` for every ACCEPTED body. Only concrete model fields are passed to the constructor now; the client's identity survives, because `umid` maps to the model's `id` through the serializer's `source`. *(2026-09-02: corrected on two points.)* The filter read `Folder._meta.get_fields()`, which also returns REVERSE relations (`Clip.folders` is listed on `Folder` under the field name `clip`) — not model fields at all. Measured on Django 5.2: `Model.__init__` accepts any name `get_field()` resolves, so a serializer key `clip` went through `Folder(**kwargs)` and landed as a STRAY ATTRIBUTE on the instance, silently — not the `TypeError` an earlier version of this row claimed. It is `_meta.concrete_fields`, which is what this row and the code's own comment always said. `validated_data` is keyed by SOURCE, not by declared name (`umid = CharField(source="id")` arrives as `id`), so the filter and `SERIALIZER_ONLY_FOLDER_FIELDS` both speak in sources. And dropped keys are LOGGED rather than swallowed only for the keys nobody DECLARED: `error` is dropped on every single request, so logging it fired the line unconditionally, which is the same as not having it — the typo it exists to surface was buried in a message an operator learns to ignore. The deliberate ones are named in `SERIALIZER_ONLY_FOLDER_FIELDS` and subtracted first. Pinned by `tests/tier2/test_ingest_discipline.py::test_the_clip_serializer_accepts_raise_exception_as_a_keyword`, `::test_an_invalid_folder_body_is_a_400_naming_the_fields`, `::test_the_endpoint_builds_a_folder_from_the_model_fields_only`, and — DB-free, at the filter itself — `tests/tier1/test_views_folder_kwargs.py::test_a_key_named_like_the_reverse_relation_is_dropped` (a `clip` key is dropped, never a stray attribute), `::test_the_dropped_log_fires_for_a_typo_and_not_for_error` (the line fires for an unknown key and not when `error` is the only non-model key) and `::test_serializer_only_fields_are_exactly_the_writable_sources_off_the_model` (`SERIALIZER_ONLY_FOLDER_FIELDS` == writable `FolderSerializer` sources − concrete `Folder` field names, computed, not guessed). |

| FR-36 | red-multi-component | 2026-09-02 | **(RED multi-component import — an un-evidenced anchor verdict is REFUSED, not guessed — Camille's ruling on D6.)** `red.anchor_yields_video_component` answered `True` on a timecode it could not read (missing, non-string, matching neither shape) — the backward-compatible declaration, and the SILENT over-declaration direction whenever the anchor was in fact binary. It now answers `None`, `getClipMainMediaFile` carries it, and `_import_multi_component` refuses to declare a budget on it BEFORE any import: `Clip.error` names the anchor and the unreadable timecode, the clip is counted failed, the reason reaches `response["errors"]`, and the item is a clean placeholder — nothing attached, nothing declared — so a corrected timecode is a fresh import. `yields_video_component` is UNCHANGED (`None` → `True`) for the count-only callers; the refusal reads the raw value through the new `main_file_verdict_is_unknown`, for which an ABSENT key is not unknown — every other provider keeps today's count — and a falsy value is a position, not a shrug. The single-component path never reads the verdict. Re-baselined pin: `tests/tier1/test_red_segment_grouping.py::test_an_unreadable_timecode_keeps_the_pre_existing_declaration` became `::test_an_unreadable_timecode_answers_unknown_and_warns` (asserts `None`, the reader's `True`, and the warning's "will be refused"). New pins: `::test_only_an_explicit_none_is_an_unknown_verdict` (absent/False/0/""/non-mapping are not unknown); `tests/tier2/test_ingest_discipline.py::test_an_unknown_anchor_verdict_refuses_the_multi_component_import` (no call at all reaches the item API), `::test_an_unknown_verdict_fails_the_clip_in_the_report_end_to_end` (counted failed, one error line, no declaration, no landing), `::test_an_unknown_verdict_does_not_touch_the_single_component_path`. Mutations run 2026-09-02, each killed: dropping the rung (2 failed); placing it after the declaration (2); absent key read as unknown (37); falsy read as unknown (16 + 12 errors); `red` answering `True` again (9); `red` answering `False` (9); the refusal returning `True` (2). |
| FR-4 | red-multi-component | 2026-09-02 | **(Test double — the stub's component budget aligned with the MEASURED landing semantics: checked at request against LANDED files, never at landing.)** Measured on prod 2026-09-02 (throwaway item, `container=1 video=1`, two video imports 93 ms apart, both accepted): both jobs `FINISHED` with no `errorMessage`/`warningMessage`, and the shape carried TWO `videoComponent`s for one declared — a job landing into a full budget ATTACHES ANYWAY. The stub had RESERVED a slot at request time (`claim_component_slots`, a `claimed` counter, refusal on `claimed >= budget`), which is STRICTER than Vidispine and had fabricated the evidence for the wrong guard (review round 5→6). `tests/portal_stub/__init__.py`: `claim_component_slots` → `refuse_if_budget_full`, which reads `landed` and writes nothing (refusal text now ends `declared {slot}={budget[slot]}, {landed} landed`), called from the same two places (`doImportToPlaceholder`, `importFileToPlaceholder`); `_land_component` attaches UNCONDITIONALLY, so a shape can end over-full; `promote_if_complete` reads `landed >= budget` per declared slot and its docstring says over-full counting as filled is UNMEASURED; no `claimed` state remains. Consequence on the production rule: re-importing a component whose job is still in flight is a SILENT DUPLICATE, not a 400 — the in-flight guards (G1, extras) are the only thing standing between a resumed run and a doubled component. Re-pinned: `tests/tier2/test_ingest_discipline.py::test_an_import_that_starts_no_job_claims_no_slot` became `::test_an_import_that_starts_no_job_consumes_nothing`; new `::test_two_in_flight_imports_are_both_accepted_and_both_land` (the measured fact itself); `::test_re_declaring_the_budget_replaces_it_and_frees_nothing` docstring reads "clearing landed"; `::test_a_one_file_clip_does_not_start_a_second_container_import` docstring corrected (the second request is accepted and lands twice, the pin counts `importFileToPlaceholder` calls). Mutations run 2026-09-02 against the aligned stub, each killed: stub checks the budget at LANDING again (1 failed); stub reserves at REQUEST again (8 failed, 3 errors); and the three production guards the stub now has to catch honestly — single-path `in_flight_jobs` wiring dropped (1), G1 reversed (2), extras in-flight branch dropped (5 + 1 error). |
| FR-36 | red-multi-component | 2026-09-02 | **(RED multi-component import — LANDED is not PROMOTED: a resumed anchor whose shape is still a placeholder is a LOUD dead end, not an ingest — D7 amended, iteration 5, Camille's option 1.)** After the wait, the branch that found the anchor's import job already running (`in_flight_component_files`) recorded that job and returned `True` — "whether the placeholder promoted is the WAIT's answer", which it never was: `_wait_for_components_to_land` reads whether the files LANDED, and only the anchor's own job evaluates the placeholder, once, against whatever had landed at that instant. The transition test (`::test_a_run_that_first_saw_no_extras_declares_when_the_next_run_sees_them`) pinned the consequence as a pass: `(ingested, failed) == (1, 0)` with a shape it asserted, in the same test, was NOT promoted. Now: `_placeholder_still_open` reads the shape through the same three-state pair as `_placeholder_file_ids`; still a placeholder → `Clip.error` = "the anchor was imported by VX-…, which has now landed, and the shape is still a placeholder — … cannot be promoted by a re-run", `False`; unreadable on both states → "cannot be read … the next run re-examines this item", `False`; promoted → the job is recorded and `True`. On neither failing arm is the job recorded: `is_incomplete_import` only revisits a job-less clip, and the anchor-attached rung then reports the item on every run until someone acts — the same operator contract as the other dead end. The transition test is re-pinned to `(0, 1)`, the reason, and `job_id is None`; the two direct in-flight pins (`::test_an_anchor_whose_job_is_still_running_is_waited_for_not_re_imported`, `::test_two_running_anchor_jobs_are_both_waited_for_and_the_first_is_recorded`) now say the anchor's job PROMOTED the shape (`_ItemHelperFake(promoted=True)`, a fake that answers ONE state of the filter rather than both — the pre-2026-09-02 fake was a shape placeholder and promoted at once). Also here: the D6 refusal of an unknown verdict is gated on `not attached_file_ids` — a resume keeps the first run's declaration and does not consult the verdict, so refusing there stranded an import whose budget WAS evidenced on a re-extraction that no longer could read the metadata; `_component_budget_report` says when its video count carries a verdict the provider could not vouch for, instead of presenting the `None`→`True` fold as a fact; the anchor-no-job message counts the extras an earlier run attached beside the ones a job covers; the refusal text is provider-neutral ("see the provider's warning for what it could not read"). Pinned by `::test_an_anchor_job_that_landed_without_promoting_is_a_loud_dead_end`, `::test_an_unreadable_promotion_state_fails_the_clip_for_the_next_run`, `::test_an_unknown_verdict_does_not_refuse_a_resume_that_declares_nothing`, `::test_the_budget_report_does_not_present_an_unknown_verdict_as_a_count`, `::test_the_anchor_no_job_message_counts_the_extras_an_earlier_run_attached`. Mutations, on tier1 + `test_ingest_discipline.py` (989 tests): promotion check dropped (`still_open = False`) — 3 failed; job recorded before the check — 3 failed; `return False if promoted else None` → `return False` — 1 failed; `not attached_file_ids and` dropped — 1 failed; the report's unknown-verdict arm dropped — 1 failed; `already_attached` not incremented — 1 failed. `USER_GUIDE.md` message 3 and message 6 updated. |
| FR-4 | red-multi-component | 2026-09-02 | **(Test double, iteration 5 — the attached-file scan is an UNMEASURED tripwire, not a fake 400; an empty file id is file-less; the fake's shape filter answers one state.)** (1) `refuse_if_budget_full`'s scan for a file already attached to ANY shape of the item raised `ComponentBudgetExceeded` with the measured 400 JSON — but the measurement (2026-09-01) was a second component of the same TYPE after the first landed, never a re-import of the same FILE, and least of all into a PROMOTED shape; the fake was inventing Vidispine's answer. It now raises `DuplicateImportOfAnAttachedFile` (a subclass, so "refused" still holds) whose text says the case is UNMEASURED and that the plugin's resume guards exist so the request is never sent; `removeItemShape` stays record-only, so a test modelling a removed shape edits `item_shapes` or trips it. (2) `ImportWithoutAFileId` fires on `not file_id`, not `is None`: a blanked `file_id` is as file-less as a null one. (3) `_land_component`'s silent drop with no placeholder shape is documented as the fake's unmeasured answer, not a behaviour to rely on; the class docstring's `default_import_response` reference and the `claim_component_slots` comment are corrected, the duplicated `_default_job_counter = 0` removed. In `test_ingest_discipline.py`, `_ItemHelperFake` takes `promoted` and answers the placeholder query on ONE state of the three-state filter (see the FR-36 row above). Pinned by `::test_the_stub_refuses_a_duplicate_import_into_a_promoted_shape` (re-pinned to the tripwire, asserts the 400 text is ABSENT) and `::test_the_stub_refuses_an_import_with_no_file_id` (now `None` and `""`). Mutations: the scan raising the 400 text — 1 failed; `is None` in the guard — 1 failed. `tests/tier1/test_views_folder_kwargs.py` drops a `force_authenticate(request, user=None)` that authenticated nothing. |
| FR-22 | red-multi-component | 2026-09-01 | **(The report cap covers BOTH failure arms, and shares one counter.)** `INGEST_ERROR_REPORT_CAP` was applied only to the non-raising arm; the `except` arm predates this story and was uncapped and outside the counter, so a folder whose every clip RAISED still flooded the operator's report — and two separate counters would have let 2 x the cap through a folder that failed both ways. One counter, one cap, one closing "N further failed clip(s)" line. It is scoped PER INGEST PASS, which is per folder and (in paged mode) per page: that is where `response["errors"]` itself lives, and a run over many folders can still print up to the cap for each, which is the intent — one wedged card must not silence the next folder's different failure. Pinned by `tests/tier2/test_ingest_discipline.py::test_the_cap_also_bounds_the_raising_arm` and `::test_the_two_failure_arms_share_one_counter`. |
